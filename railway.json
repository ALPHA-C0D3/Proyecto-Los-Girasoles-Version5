{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd BackEnd && npm install && node config/database.js && node setup.js && node agregarcampos.js && node crearAdmin.js && node server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}